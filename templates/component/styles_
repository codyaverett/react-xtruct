<%_if(stylesExtension === 'styl'){-%>
.h1
    text-align: center
    font-size: 24px

.logo
    width: 550px
    height: 150px

.alignCenter
    text-align: center
<%_}-%>
<%_if(stylesExtension !== 'styl'){-%>
.h1 {
    text-align: center;
    font-size: 24px;
}
.logo {
    width: 550px;
    height: 150px;
}

.alignCenter{
    text-align: center;
}
<%_}-%>
