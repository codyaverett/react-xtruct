<% if(stylesExtension === 'styl') { %>.h1
    text-align: center
    font-size: 24px

.logo
    width: '150px'
    height: '150px'

.alignCenter
    textAlign: 'center'
<% } %><% if(stylesExtension !== 'styl') { %>.h1 {
    text-align: center;
    font-size: 24px;
}
.logo {
    width: '150px';
    height: '150px';
}

.alignCenter{
    textAlign: 'center';
}
<% } %>
