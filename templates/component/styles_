<%_if(stylesExtension === 'styl' || stylesExtension === 'sass'){-%>
.h1
    text-align: center
    font-size: 24px

.logo
    width: 600px
    height: 150px

.alignCenter
    text-align: center
<%_}-%>
<%_if(stylesExtension !== 'styl' && stylesExtension !== 'sass'){-%>
.h1 {
    text-align: center;
    font-size: 24px;
}

.logo {
    width: 600px;
    height: 150px;
}

.alignCenter{
    text-align: center;
}
<%_}-%>
