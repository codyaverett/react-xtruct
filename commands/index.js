'use strict';

module.exports = {
    new: require("./new"),
    install: require('./install'),
    git: require('./git'),
    generate: require('./generate'),
    build: require('./build'),
    serve: require('./serve'),
};