'use strict';

module.exports = {
    new: require("./new"),
    install: require('./install'),
    git: require('./git'),
    generate: require('./generate'),
    lint: require('./lint'),
    test: require('./test'),
    build: require('./build'),
    serve: require('./serve'),
};