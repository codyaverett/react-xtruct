'use strict';

module.exports = {
    new: require('./new'),
    install: require('./install'),
    repository: require('./repository'),
    generate: require('./generate'),
    lint: require('./lint'),
    test: require('./test'),
    build: require('./build'),
    serve: require('./serve'),
    set: require('./set')
};